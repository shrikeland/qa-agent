# QA-сводка за 2026-07-21

Команда: Mia. Статусы: Ready for Test, Testing.

Ready for Test: три задачи, все новые (ключей не было в `state.json`). Testing: задач нет.

- **MIA-87** (Bug) — кнопка «Подсказка» в тренажёре не выдавала авторские подсказки из справочника (побочный эффект fail-closed выдачи из MIA-83) — теперь gateway обогащает задание с сервера. Код — MR !278, ветка `feat/MIA-87-authored-hints-server-side`.
- **MIA-83** (Bug, High, security) — правильный ответ/эталонное решение/подсказки утекали студенту по трём каналам (HTTP `/assignments*`, HTTP submission/interactions, WebSocket) плюс отдельный debug-оверлей по 4 кликам на аватар Мии — закрыто fail-closed белым списком полей и новым гейтованным эндпоинтом результатов диагностики. Код — MR !276, ветка `feat/MIA-83-assignment-exposure`.
- **MIA-86** (Feature) — методист заводит эталонное пошаговое решение в справочнике, проверка в тренажёре (доска и текст/чат) опирается на него, ученику эталон не показывается. Скоуп ограничен тренажёром — диагностику не затрагивает. Код — MR !275, ветка `feat/solution-steps-grading`.

Все три ветки-источники были влиты и удалены (squash-merge) до чтения кода — читались по коммитам-слияниям (`merge_commit_sha`) в дереве после мержа; на статус мержа ветка/MR внимания не обращали, это обычная практика проекта.

| Ключ | Область/статус | Решение | Заметка |
|------|-----------------|---------|---------|
| MIA-87 | Ready for Test — баг: authored-подсказки в чате тренажёра | 🆕 новый | [[MIA-87]] |
| MIA-83 | Ready for Test — баг/security: утечка ответа задания через API | 🆕 новый | [[MIA-83]] |
| MIA-86 | Ready for Test — фича: эталонное решение в проверке тренажёра | 🆕 новый | [[MIA-86]] |

Итого: 🆕 3 · ♻️ 0 · ✅ 0.

## Технический комментарий к запуску

Заметки MIA-83 и MIA-86 готовились параллельно двумя фоновыми агентами в общем клоне `/tmp/repo` — один из агентов (MIA-86) обнаружил, что рабочее дерево мутировало параллельным `git checkout` другого агента прямо во время чтения файлов, изолировал себе отдельный клон в scratchpad и перепроверил все ключевые файлы там. Итоговые факты в обеих заметках выборочно сверены заново (whitelist полей в `assignment-privacy.ts`, коды 403/404 и условие `finishedAt` в `getDiagnosticSessionResultAssignments`, удаление debug-оверлея `miaClicks > 3`) напрямую по коммиту `merge_commit_sha` MIA-83 — расхождений не найдено.

Заметка запушена в ветку `claude/kind-knuth-svr8wr` (репозиторий этой сессии настроен на выделенную ветку, прямой пуш в `main` не выполнялся) — см. также Telegram-сводку.
