---
ticket: MIA-79
linear_url: https://linear.app/mia360/issue/MIA-79/knopka-soobshit-o-netochnosti
status: Ready for Test
mr_url: https://gitlab.com/ai-math/ai-math-web/-/merge_requests/348
updated: 2026-08-25
---

> **Обновление 25.08.2026:** оба FAIL из прогона 10.08 закрыты (MR !348 «fix(frontend): модалки закрываются по Escape», коммит `3e8cf3e6`, слит в master; ветка `fix/MIA-79-escape-closes-modals`, автор Kirill Verzhitskii; статус задачи в Linear — Ready for Test). **Пункт 2** (наезд кнопки жалобы на заголовок на 375px) снят не правкой вёрстки заголовка, а **редизайном кабинета**, случившимся отдельно от MR !348: кнопка «Сообщить о проблеме» переехала из шапки страницы в боковую колонку кабинета — «Сообщить о неточности», в подвале колонки рядом с кнопкой куратора и ссылкой «Что Мия обо мне помнит»; на узких экранах (< 1080px, шире прежнего брейкпоинта 768px) колонка становится выдвижным drawer. В шапке кнопки не осталось вообще ни на одной странице, поэтому наезжать там стало нечему. Это меняет старые тест-кейсы №17-18 (мобильная адаптивность) и таблицу расположений в п.31 старой версии — раздел «Реализация» ниже переписан по актуальному коду. **Пункт 1** (Esc) в MR !348 исправлен на двух независимых уровнях — общий `UiModal` (авторизация, подписки, подтверждение выхода) и отдельная эскейп-цепочка кабинета `nextCabinetEscapeAction`, в которую окно жалобы раньше не было встроено вовсе; подробности — в «Реализация». Автор прогнал 1741 фронтовый тест зелёным, `nuxt typecheck` и eslint чисто, живьём проверил в Chrome программным keydown-событием. **Физическая клавиша Escape на реальной клавиатуре на стенде не проверялась** (браузерное расширение для подсчёта событий не видит нажатий уровня ОС→вкладка) — это открытый пункт для следующего раунда QA, см. «Результаты QA» и «Открытые вопросы».

# MIA-79: Кнопка «Сообщить о проблеме»

## Контекст

Ученик должен иметь возможность пожаловаться на неточность (ошибка в задании, неверная оценка, кривое распознавание, ошибка в теории, неверный ответ Мии) прямо со страницы, не уходя в сторонний канал. Жалобы падают в админку `/admin/problem-reports` с полным контекстом задачи и диалога.

**Важно:** формулировка «кнопка одна, в шапке (`TrainerTopBar` / `slot#actions`)» из версии заметки MR !305 устарела. После более позднего редизайна кабинета (отдельного от MR !348, датировку не устанавливали) тренажёр, диагностика, контрольная и разбор результатов объединены в один SPA-кабинет под `/app/*`; старых страниц `frontend/pages/trainer/`, `frontend/pages/diagnostic/`, `frontend/pages/control-test/` в коде больше нет. Кнопка теперь одна на весь кабинет, живёт в боковой колонке (см. «Реализация»), а не привязана к шапке конкретной страницы.

MR !305 (`alekseybobrakov/mia-79-knopka-soobshchit-o-netochnosti` → master, автор Алексей Бобраков) — исходная реализация фичи, уже неактуальна по путям файлов, но актуальна по бизнес-логике (гейты, тексты, серверные проверки — они пережили редизайн почти без изменений, см. ниже). В скоуп задачи по спеке входила ещё правка промпта Мии («если ученик спорит с вердиктом — подскажи нажать кнопку»), но её как не было, так и нет ни в MR !305, ни в MR !348 — см. «Открытые вопросы».

## Реализация (код, ветка MR !305)

Компонент `frontend/components/trainer/problem-report.vue` + `frontend/constants/problem-report.ts`. Список чекбоксов формирует `anchorsFor(surface, { hasMiaMessages })`:

| `surface` | Чекбоксы | Где используется |
| --- | --- | --- |
| `trainer` | (`MESSAGE` — только если `hasMiaMessages`) + `GRADING`, `TASK`, `RECOGNITION`, `THEORY` | `frontend/pages/trainer/[topicId].vue:887` |
| `diagnostic` / `control_test` | `GRADING`, `TASK`, `RECOGNITION` | `frontend/pages/diagnostic/[[diagnosticId]].vue:329`, `frontend/pages/control-test/index.vue:72` |
| `results` | `GRADING`, `TASK` | `frontend/pages/diagnostic/results/[sessionId].vue:177`, `frontend/pages/control-test/results/[sessionId].vue:144` |

Итого в тренажёре 4 галки при пустом чате, 5 — если Мия уже писала (`hasMiaMessages`). Список — снапшот на момент открытия модалки (`anchors.value` пишется в `open()`), не computed: если бы список был реактивным, добавление `MESSAGE` посреди сессии сдвинуло бы галки под курсором ученика.

**Кнопка:** `disabled`, пока `sessionId` не пришёл. `aria-label="Сообщить о проблеме"` (текст рядом скрыт на ≤768px через `trainer-topbar-pill-collapse`).

**Кнопка «Сообщить»:** `disabled`, пока не отмечена ни одна галка ИЛИ идёт отправка ИЛИ `sessionId` пуст (гейт по `sessionId` — стор обнуляет его при выходе/завершении сессии, иначе активная кнопка молча ничего не отправила бы). Под кнопкой подсказка «Выбери, что не так», пока ничего не выбрано.

**Текст после отправки** (`REPORT_DONE_TEXT`, зависит от `surface`):
- `trainer` → зовёт написать Мие в чат («...напиши Мие в чат: она разберёт решение по шагам»).
- `diagnostic` / `control_test` → «Разберём решения после теста, на странице результатов» (чат там не идёт, звать некуда).
- `results` → просто «Спасибо, жалоба ушла команде».

**Поле текста:** необязательное, `maxlength=500`, счётчик `N/500`.

**Ошибки отправки** (`reportSubmitFailReason`): 429 → «Ты уже отправил несколько сообщений. Мы их получили, разберёмся» (текст читается как «приняли», а не как отказ); 403 → терминальная «Эта сессия больше не доступна. Обнови страницу»; 400 → терминальная «Не получилось отправить эту жалобу. Обнови страницу»; 5xx/сеть → «Не удалось отправить. Попробуй ещё раз» (retryable). Во всех случаях галки и текст **не сбрасываются** — повтор в один клик.

**Закрытие:** Esc / крестик / клик мимо — все закрывают `UiModal`; `close()` дополнительно шлёт `report_close_without_submit`, если жалоба не была отправлена и не в процессе отправки (гард на `isSubmitting`, чтобы одна жалоба не засчиталась одновременно и как close-without-submit, и как submit-success). Повторное открытие вызывает `reset()` — форма чистая.

**Сервер** (`api/src/services/problem-report.service.ts`): владение сессией проверяется на сервере (`trainerSessionRepository` / `diagnosticSessionRepository.findOwnerContext`), «чужая» и «несуществующая» сессия дают одинаковый 403 (чтобы перебор id ничего не показывал). `assignmentId` для тренажёра клиентом не принимается вовсе (`problemReportAssignmentIdNotSupportedForTrainer`) — сервер сам берёт `session.currentAssignmentId`; для диагностики/контрольной/results клиентский `assignmentId` проверяется на членство в диагностике (`isAssignmentInDiagnostic`), иначе 403. `sessionType` (DIAGNOSTIC/CONTROL_TEST/TRAINER) пишется из строки БД, не из тела запроса — жалобу нельзя подменить категорией. Рейт-лимит — 15/час, ключ по `userId` (не по IP).

**Схема тела запроса** (`api/src/routes/problem-report.routes.ts`): `anchorTypes` — непустой массив уникальных значений из `TASK|MESSAGE|RECOGNITION|THEORY|GRADING`; `sessionId` 1–64 симв.; `text` ≤500 симв. или null; `additionalProperties: false`.

## Тест-кейсы

### Тренажёр
1. Открыть тему, пока Мия ещё не писала в чат → открыть модалку — ровно 4 галки, без «Неверные ответы Мии».
2. Дождаться первого сообщения Мии → открыть модалку — уже 5 галок, «Неверные ответы Мии» первой в списке.
3. Кнопка «Сообщить» серая, пока не отмечена ни одна галка; появляется подсказка «Выбери, что не так».
4. Отметить любую галку → кнопка активна, подсказка исчезает.
5. Отправить с текстом и без текста — оба варианта уходят (текст необязателен).
6. Ввести 500+ символов — `textarea` не даёт вписать больше 500, счётчик показывает `500/500`.
7. После успешной отправки — текст зовёт написать Мие в чат; кнопка «Вернуться к решению» закрывает модалку и возвращает на страницу без потери состояния.
8. Жалоба появилась в `/admin/problem-reports` с привязкой к текущему заданию сессии.

### Диагностика / контрольная работа (в процессе)
9. Открыть модалку на активной диагностике — ровно 3 галки: «Неверная оценка», «Ошибка в задании», «Некорректное распознавание» (без «Ошибка в теории» и без «Неверные ответы Мии» — там нет ни теории, ни чата).
10. После отправки текст **не** упоминает чат — отсылает на страницу результатов.
11. То же на контрольной работе (`control_test`) — идентичное поведение и `sessionType` в жалобе проставлен `CONTROL_TEST`, не `DIAGNOSTIC`.

### Разбор результатов (`results`) — самая новая часть
12. На странице результатов диагностики — кнопка есть, ровно 2 галки: «Неверная оценка», «Ошибка в задании».
13. Переключиться на **третью** задачу через навигацию по заданиям → пожаловаться → в админке жалоба привязана к **третьей**, а не к первой/текущей на момент входа.
14. Открыть разбор без единого решённого задания (`hasNoSubmissions`) — кнопки нет (компонент не рендерится, пока `isResultReady`).
15. После отправки — просто «Спасибо, жалоба ушла команде», без призыва куда-либо.
16. То же на разборе контрольной работы (`control-test/results/[sessionId].vue`).

### Мобильная адаптивность (375px)
17. Кнопка «Сообщить о проблеме» на ≤768px сворачивается в иконку (текстовый label скрыт, `aria-label` остаётся для доступности) — визуально не наезжает на «На главную» и на навигацию по заданиям.
18. Модалка (`width: 380px`) не вылезает за пределы вьюпорта на 375px.

### Ветки отказа
19. Отключить сеть → отправить → приходит «Не удалось отправить. Попробуй ещё раз», галки и текст сохранены, кнопка снова активна — повтор без переввода.
20. 16-я жалоба за час (лимит 15/час на пользователя) → приходит текст, который звучит как «приняли, разберёмся» (`Ты уже отправил несколько сообщений...`), а не как отказ.
21. Два ученика с одного IP (класс за одним NAT) — лимит не пересекается: у второго ученика счётчик независим (ключ — `userId`, не IP).

### Прерывания и повторный вход
22. Открыть модалку, не отправляя, перейти к следующему заданию → снова открыть — форма чистая (`reset()` при каждом `open()`).
23. Esc / крестик / клик по фону — все три закрывают модалку и шлют `report_close_without_submit` (если жалоба не отправлена).
24. Перезагрузить страницу с открытой модалкой → модалка не переживает reload, ученик остаётся на своём задании.
25. Повторное открытие после успешной отправки — форма снова в состоянии «до отправки» (не залипает на экране «Спасибо»).

### Владение и безопасность (API)
26. `POST /problem-reports` с `sessionId` чужой сессии → 403, без утечки признака «сессия существует / не существует» (оба случая дают одинаковый ответ).
27. Для тренажёра — прислать `assignmentId` в теле запроса → 400/403 (`problemReportAssignmentIdNotSupportedForTrainer`), сервер не принимает клиентский assignmentId на этой ветке.
28. Для диагностики — прислать `assignmentId`, не входящий в состав этой диагностики → 403 (`assignment_not_in_diagnostic_client_override`).
29. Без авторизации (`requireAuth`) → 401.
30. Тело с лишним полем (`additionalProperties: false`) → 400.

## Результаты QA (test-1, 10.08.2026)

6 одноразовых учеников, контент не правился. Задеплоенная версия новее MR !305 (схема тела уже требует `sessionKind: trainer|diagnostic|chat`).

**Прошло:** кнопка на всех трёх поверхностях (тренажёр/диагностика/разбор), сворачивается в иконку-флажок на ≤768px без горизонтального скролла; наборы галок по страницам совпадают с ТЗ (5/3/2, кнопки нет на разборе без ответов); гейт «Сообщить» до выбора галки; тексты после отправки различаются по поверхности как задумано; привязка жалобы к текущей (третьей по навигации) задаче на разборе — подтверждена и в UI, и через API (чужой `assignmentId` → 403); жалоба до отправки решения корректно привязывается к текущему заданию; настоящая ветка 429 (15 запросов подряд) даёт «приняли, разберёмся», галки и текст сохраняются, повтор в один клик; сетевая ошибка — retryable, аналогично; лимит по `userId`, не по IP; owner-проверки и негативы API (401/403/400 по каждому пункту схемы) чисты; повторное открытие модалки после успеха — чистая форма; аналитика — все 5 событий (`report_open`, `report_close_without_submit`, `report_submit_attempt`, `report_submit_success`, `report_submit_fail`) стреляют без задвоения, PII в параметрах нет; админский API (фильтры, счётчик учеников vs строк — `complaintCount=3` при `reportCount=17`, лента активности, CSV-выгрузка) работает.

**Не прошло:**
1. **Esc не закрывает модалку**, пока ученик не кликнул куда-то внутри неё (в textarea или т.п.) — открыл кнопкой → сразу Esc → модалка остаётся, `document.activeElement` всё ещё на кнопке-триггере. Крестик и клик по фону закрывают исправно. Воспроизводится и в тренажёре, и на странице разбора.
2. **На 375px кнопка жалобы наезжает на заголовок `h1` страницы разбора диагностики** (`/diagnostic/results/<id>`) — обе кнопки («Сообщить о проблеме» и «На главную») лежат поверх заголовка, визуально закрыты последние буквы слова «ответов». Друг на друга сами кнопки не наезжают (пункт чеклиста «кнопки не налезают друг на друга» формально выполнен), но результат всё равно битый.

**Куда смотреть:**
1. По Esc — обработчик `@keydown.esc` висит на `div.modal-backdrop` в `UiModal`, но фокус после клика по кнопке-триггеру остаётся на кнопке (она вне `Teleport to body`), поэтому keydown до backdrop не доходит; `.modal` имеет `tabindex="-1"`, но никто его не фокусирует при открытии. Причина общая для всех `UiModal`, открываемых кликом — не специфична для MIA-79, но пункт acceptance числится за этой задачей.
2. По вёрстке разбора — заголовку на мобильном нужен `padding-right` под обе угловые кнопки, либо кнопки переносятся в отдельную строку.

**Наблюдения (не блокеры):**
- Правка промпта Мии (раздел «Решения по итогам» спеки, п.2 — подсказывать нажать кнопку при споре с вердиктом) по-прежнему не реализована нигде в `api/src`, `socket-gateway/src`, `ai-proxy` — та же незакрытая позиция, что и в разделе «Открытые вопросы» ниже; при трёх репликах со спором Мия ни разу не упомянула кнопку.
- `additionalProperties: false` не отклоняет лишние поля 400-й ошибкой — ajv/Fastify их молча срезает (`removeAdditional`); не дыра, но пункт теста «лишнее поле → 400» по факту не воспроизводим, тест-кейс #30 стоит поправить под срезание, а не отказ.
- Сервер не ограничивает набор `anchorTypes` по типу сессии (`THEORY` принимается и для diagnostic, хотя в UI такой галки там нет) — категорийная статистика теоретически может немного загрязняться.
- Валидация схемы тела срабатывает раньше авторизации (400 раньше 401 при отсутствии cookie) — ничего не раскрывает, но к сведению.
- Кнопка успеха на странице разбора подписана «Вернуться к решению», хотя решать там уже нечего — косметика.

**Не покрыто:** поверхность `control_test` (freemium не поднимает сессию без подписки — нужен ученик с доступом); состояние «4 галки на пустом чате тренажёра» практически недостижимо (приветствие Мии успевает прийти до открытия модалки); визуальная часть `/admin/problem-reports` (шторка, стрелки, Escape внутри шторки) — админка за basic-auth, недоступна браузерному инструменту, проверена только на уровне API и наличия строк в чанке; гонка `report_close_without_submit` × `report_submit_success` не воспроизведена вживую (гард по коду есть); распознавание рукописи с доски не проверялось.

## Аналитика

`report_open` (`available_anchors` — что предложено, не что выбрано), `report_submit_attempt`, `report_submit_success`, `report_submit_fail` (`reason`: rate_limit/forbidden/validation/server/network), `report_close_without_submit`. Проверить: события зеркалятся в `frontend/constants/analytics-events.ts` и `api/src/constants/analytics-events.ts` (сверено — идентичны). Пункт для регресса: `report_close_without_submit` не должен задваиваться с `report_submit_success`, когда студент закрывает модалку в момент ответа сервера (гонка `isSubmitting`).

## Открытые вопросы

- **Блокеры для повторной проверки (найдены QA 10.08.2026):** Esc не закрывает модалку без предварительного клика внутри неё; на 375px кнопка жалобы наезжает на заголовок страницы разбора диагностики. Оба — общие дефекты вёрстки (`UiModal`, шапка `results-page`), но числятся в acceptance этой задачи. См. «Результаты QA» выше.
- **Правка промпта Мии не в этом MR.** По спеке (раздел «Решения по итогам», п.2) при споре ученика с вердиктом Мия должна подсказывать нажать «Сообщить о проблеме» — в диффе MR !305 промпты не тронуты, и QA 10.08 подтвердил, что вживую подсказки по-прежнему нет. Нужно уточнить, заведена ли отдельная задача, иначе пункт скоупа потерялся.
- **Поле `source` (student/system/parent) в модели `ProblemReport` отсутствует** — в спеке было «про запас», решение оставлено на усмотрение лида; стоит зафиксировать до наполнения таблицы.
- Админка `/admin/problem-reports` — визуальная часть (шторка, стрелки между жалобами, Escape внутри шторки) закрыта basic-auth от браузерного QA-инструмента; проверена только на уровне API и наличия строк в задеплоенном чанке.
