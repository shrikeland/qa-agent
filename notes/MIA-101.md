---
ticket: MIA-101
linear_url: https://linear.app/mia360/issue/MIA-101/tajmingi-ai-zaprosov-dlitelnost-ishod-i-alert-na-tajmauty
status: Ready for Test
mr_url: https://gitlab.com/ai-math/ai-math-web/-/merge_requests/306
updated: 2026-08-20
---

# MIA-101: тайминги AI-запросов — длительность, исход и алерт на таймауты

## Контекст

До фичи длительность и исход AI-запроса были видны только частями: у проверки решения с доски — в базе, у чата — в метриках без привязки к ученику, у вызовов из api (приветствие, память Мии, отчёт родителю) — вообще никак. Неудачные вызовы (таймаут/ошибка) не попадали в учёт совсем, поэтому нельзя было увидеть, что медиана генерации приветствия (2.9 с) стоит на грани лимита в 3 с и каждый четвёртый вызов срывается в шаблон.

Это уже третий раунд. Первый MR (!306, `feat/MIA-101-ai-request-timings`, коммит `88f76dff`) завёл тайминги/исход в общем учёте и на дашборде `/admin/ai-cost`. QA-прогон 31.07 нашёл три расхождения — все три (разрез по ученику, видимость `error`, запись неудачи отчёта родителю) закрыты коммитами `d33f70fb`/`f5bde952`. QA-прогон 18–20.08 (после `master@2793ba2f`) подтвердил все три закрытыми, но нашёл два новых пробела в «неудачные попытки попадают в учёт»: автоаборт распознавания по таймауту и `kind='ocr'` — см. «Реализация» и открытые пункты в тест-кейсах 4 и 5. Ветка задачи (`andreichepurchenko/mia-101-...`) на момент этого прогона в GitLab не существует и MR под неё не найден — все правки (`88f76dff`, `d33f70fb`, `f5bde952` и последующие) уже влиты в `master`; код фичи читан прямо с `master` (актуальный HEAD на момент прогона), это ожидаемо и не признак «код не найден».

## Реализация

Проверено чтением файлов на текущем `master` (см. «Контекст» — ветки под задачу больше нет, все раунды уже влиты).

**Закрыто (round 1–2, подтверждено на round 3).**
- Чат и распознавание (`socket-gateway`): `socket-gateway/src/helpers/ai-usage.helper.ts` → `recordStreamAiUsage`/`recordFailedCall` пишет запись без `usage`/стоимости, но с `status`/`durationMs`, когда `finish()` вызван с `failure`. `conversation.stream.ts` зовёт `finish(undefined, 'error')` на ошибке прокси и `finish(undefined, 'timeout')` на авто-аборте (`AiStreamAutoAbortTimeout` = 120 с) — обе ветки размечены и пишутся.
- Отчёт родителю: `api/src/services/parent-report.service.ts` → `recordParentReportUsage` теперь зовётся безусловно (строки с диагностическим/недельным/месячным отчётом), а не только внутри `if (summaryResult)` — неуспех (`outcome.ok === false`, `status: 'timeout' | 'error'`) тоже попадает в `ai_usage`. Единственный случай без записи по-прежнему `status === 'skipped'` (не задан ключ провайдера — запрос никуда не уходил).
- Разрез по ученику: `api/src/admin/ai-cost.service.ts` → `breakdownForParent` отдаёт `p50Ms`/`p95Ms`/`timeouts`/`errors` по `surface`/`kind` (раньше только `usd`/`calls`). `frontend/components/admin/admin-user-ai-cost.vue` — колонка «Медиана / 95%», бейджи `{{ r.timeouts }} таймаут.` / `{{ r.errors }} ошиб.` (строки 74–79), тултип `` `Медиана ${p50Ms} мс, 95-й процентиль ${p95Ms} мс, таймаутов ${timeouts}, ошибок ${errors}` ``.
- Исход `error` виден в UI: и в карточке ученика, и на общем дашборде (`frontend/pages/admin/ai-cost/index.vue`) счётчик `errors` рядом с `timeouts`.

**Не закрыто — новые находки round 3, актуальны на момент этого прогона.**

1. **Автоаборт распознавания не пишет исход.** `socket-gateway/src/sockets/streams/recognition.stream.ts:129-134`, метод `abort()`: `this.finish();` — без второго аргумента. Для сравнения `conversation.stream.ts:165-173` в `abort()` зовёт `this.finish(undefined, 'timeout')`. В `recordStreamAiUsage` ветка без `endPayload` пишет запись только когда передан `failure`, поэтому зависшее до `AiStreamAutoAbortTimeout` (120 с) распознавание не оставляет в `ai_usage` ни `timeout`, ни `error` — асимметрично `chat`, где та же ветка размечена. Правка симметрична уже сделанному для чата: заменить `this.finish()` на `this.finish(undefined, 'timeout')`.
2. **`kind='ocr'` не пишет `durationMs` (и `status` при неуспехе) в двух из трёх мест.** `api/src/services/mathpix.service.ts:117-125` (модель `mathpix-strokes`) и `api/src/services/whiteboard-photo.service.ts:73-84` (`mathpix-text`) зовут `aiUsageService.record({...})` без полей `durationMs`/`status` — только `homework.service.ts:481` (`_readPageLines`) считает `Date.now() - startedAt` и передаёт `durationMs`. У обоих недостающих мест колонка времени в UI будет прочерком, неотличимым от строк до миграции.

**Общее поведение (не менялось в этом раунде, проверить как регресс):**
- `p50Ms`/`p95Ms` = 0 (или отсутствие данных) рендерится как `-`, а не `0.0 с` — важно для старых записей до миграции (`duration_ms = NULL`) и сейчас неотличимо от находки 2 выше (`ocr` тоже даёт 0).
- Доля таймаутов считается одним SQL-запросом (`COUNT(*) FILTER (WHERE status = 'timeout')`), без похода в логи.
- Перцентили (`percentile_cont`, `ai-cost.service.ts`) по-прежнему считаются по всем строкам периода без `FILTER (WHERE status = 'ok')` — таймауты и ошибки подмешиваются в медиану/p95. Не блокер, но искажает картину: окно с одним таймаутом приветствия даёт `p50 = p95 = 3001 мс` (= `TIMEOUT_MS`).
- Склонение `N таймаут.` / `N ошиб.` не учитывает число (при 5 будет «5 таймаут.», «5 ошиб.») — косметика, не блокер.
- Алерт на рост доли таймаутов — **вне поставки этого тикета**, настраивается отдельно на monitoring-хосте после того, как метрика доедет до прода.

## Тест-кейсы

### Приоритет: два открытых пробела round 3

1. **[ОТКРЫТО] Автоаборт распознавания должен попадать в учёт.** Спровоцировать зависание `recognition`-стрима на 120+ с (отправка решения фото/текстом с недоступным/подвешенным ai-proxy — если на стенде нет доступа к контейнерам, проверить хотя бы по коду/юнит-тестам `ai-usage.helper.test.ts`, что ветка теста на этот случай вообще есть).
   Ожидание (текущее, до фикса): записи в `ai_usage` не появится вовсе — ни `timeout`, ни `error`; в дашборде/карточке ученика по `recognition` не будет расти ни `calls`, ни `timeouts`. Это баг, а не ожидаемое поведение — после фикса (`recognition.stream.ts` → `this.finish(undefined, 'timeout')`) ожидание меняется на «строка появляется с `status='timeout'`, `usd=0`», как уже работает для `chat`.
2. **[ОТКРЫТО] `kind='ocr'` должен показывать тайминги.** Пройти OCR через оба непокрытых пути: распознавание по фото на доске (`whiteboard-photo.service.ts`, surface зависит от `input.kind`) и flow через Mathpix strokes (`mathpix.service.ts`, если доступен на стенде — ввод рукописного решения). Сверить с third path `homework/ocr` (загрузка фото домашки), который уже пишет длительность.
   Ожидание (текущее, до фикса): у `trainer/ocr` и `diagnostic/ocr` в дашборде/карточке — прочерк в колонке времени, неотличимый от строк до миграции; у `homework/ocr` время отображается. После фикса (добавить `durationMs` в оба `record(...)`) все три `ocr`-строки должны вести себя одинаково.

### Регресс: закрытые в round 1–2 пункты (проверить, что не отвалились)

3. **Тайминги и исход у покрытых типов вызовов.** Под свежим учеником пройти: `greeting` (вход в тему), `chat` (пара сообщений Мии), `recognition` (отправка решения текстом — happy path, не автоаборт), `memory_extraction` (пара сессий с чатом), `parent_report` (сформировать отчёт, если доступно на стенде).
   Ожидание: в таблице «по фиче/типу» на дашборде и в карточке ученика у каждой строки — медиана и 95-й процентиль (`X.X с / Y.Y с`), не прочерк (кроме `ocr`, см. п.2).
4. **Таймаут отображается.** Поймать таймаут `greeting` (лимит 3 с, ловится на живом трафике при частичной загрузке).
   Ожидание: у строки красным `N таймаут.`, число вызовов растёт, `usd` не растёт от неудачного вызова (стоимость без токенов).
5. **Ошибка отображается.** Спровоцировать `status='error'` (недоступный провайдер/сетевая ошибка на `chat`) — если недостижимо руками, свериться по юнит-тестам `ai-cost.service.test.ts`.
   Ожидание: рядом с таймаутами — красным `N ошиб.`, тултип показывает «таймаутов N, ошибок M» раздельно.
6. **Отчёт родителю: неуспех тоже пишется.** Спровоцировать сбой генерации сводки (истекший таймаут/недоступный провайдер) для диагностического, недельного или месячного отчёта.
   Ожидание: строка `parent_report` получает `status='timeout'`/`'error'`, а не пропадает из учёта; случай `skipped` (нет ключа провайдера) по-прежнему не пишется — это ожидаемо, не баг.
7. **Пустые/старые данные не ломают вид.** На периоде только со старыми записями (до миграции, `duration_ms=NULL`) — колонка времени показывает `-`, не `NaN`; на смешанном периоде перцентиль считается по новым, старые не искажают картину.

### Админка: карточка ученика `/admin/users/:parentId`

8. **Тайминги и исход видны по конкретному ученику.** Карточка ученика с историей вызовов (в т.ч. с пойманным таймаутом/ошибкой) → в блоке «AI-расходы» у каждой фичи медиана/95% и, если есть, `N таймаут.`/`N ошиб.` — так же, как на общем дашборде.
9. **Пустое состояние.** Ученик без единого вызова AI → «Вызовов AI ещё не было», без пустой таблицы и ошибок в консоли.
10. **Расхождение с дашбордом отсутствует.** Для одного и того же вызова тайминг/исход в карточке ученика и в общем дашборде за тот же период совпадают (перцентили считаются раздельными SQL-запросами — риск разъехаться при рассинхроне).

### Регресс / общее

11. **Грейдинг и функциональность не задеты.** Тренажёр и диагностика продолжают штатно проверять решения — фича только добавляет учёт, не меняет бизнес-логику вызовов.
12. **Деплой без простоя.** Не ручной тест-кейс, но стоит свериться с DBA/автором: миграция `expand-only` (nullable-колонки, `CREATE INDEX` без `CONCURRENTLY` — на текущем объёме некритично, но на большей проде-таблице стоит перепроверить время построения).
13. **`/api/metrics` (Prometheus).** Гистограмма `ai_request_duration_seconds` с лейблами `kind`/`model`/`status` — второй независимый канал; для случаев из п.1, 4–6 метка `status` должна совпадать с записью в БД. Заметка не по скоупу задачи, но встретится по пути: эндпоинт отдаётся без авторизации.

## Аналитика/API

- Фича не добавляет пользовательских аналитических событий (`analytics-events.ts`) — это внутренняя админ-метрика, событий в `frontend/constants/analytics-events.ts` / `api/src/constants/analytics-events.ts` проверять не нужно.
- Для удобства тестирования полезны: `GET /api/admin/ai-cost` (общий дашборд, JSON) и `GET /api/admin/users/:parentId` → поле `aiUsage.bySurfaceKind` (карточка ученика) — оба под basic-auth админки.

## Открытые вопросы

- **Два известных бага round 3 ещё не исправлены** (см. тест-кейсы 1–2): автоаборт `recognition` не пишет исход; `kind='ocr'` не пишет `durationMs` в двух из трёх мест. Задача вернулась в «Ready for Test» после фикса предыдущих трёх расхождений — вероятно, эти два ждут очередного раунда правок перед повторной проверкой. Смысла гонять QA-прогон по всему скоупу заново нет, пока не будет подтверждения, что оба пункта исправлены — но регресс-пункты (3–13) можно и стоит проверять параллельно.
- Алерт на рост доли таймаутов — отдельный шаг вне этого тикета (настраивается на monitoring-хосте вручную), тестировать в рамках MIA-101 не нужно.
- Живой таймаут/ошибку провайдера для `recognition`/`parent_report` со стороны QA не всегда получается спровоцировать вручную (нужно уронить/подвесить ai-proxy) — если доступа к контейнерам стенда нет, опираться на код и юнит-тесты (`ai-usage.helper.test.ts`, `parent-report-ai.service.test.ts`), явно фиксируя это в отчёте.
- Из наблюдений прошлых прогонов (не блокеры, держать в уме): перцентили смешивают `ok`/`timeout`/`error` (нет `FILTER (WHERE status='ok')`); склонение «N таймаут.»/«N ошиб.» не учитывает число; `/api/metrics` отдаётся без авторизации; `/api/internal/ai-usage` без HMAC отвечает 400 с текстом ошибки схемы раньше проверки авторизации; запись `recordParentReportUsage` на неуспехе не покрыта отдельным тестом. Эти пункты не входят в скоуп MIA-101 и чинить/проверять их здесь не требуется — упомянуты только для контекста, если попадутся на глаза при прогоне.
